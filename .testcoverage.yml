# Test coverage configuration for go-test-coverage
# See: https://github.com/vladopajic/go-test-coverage

# Profile path (will be generated by go test)
profile: coverage.out

# Local mode - for local testing and CI/CD
local: true

# Coverage thresholds (starting low for new project, increase over time)
# TODO: Gradually increase these as more tests are added:
# - Target: total: 80, package: 75, file: 70
# - Current: total: 40, package: 20, file: 20 (v0.1.x baseline)
threshold:
  # Global minimum coverage percentage
  total: 40
  # Package minimum coverage percentage
  package: 20
  # File minimum coverage percentage
  file: 20

# Override thresholds for specific packages/files
override:
  # Allow lower coverage for main packages (they're usually just entry points)
  - threshold: 0
    path: ^github\.com/rafa-dot-el/mcp-shell/cmd/mcp-shell$
  # Allow lower coverage for cmd/mcp-shell package (alternative path format)
  - threshold: 0
    path: ^cmd/mcp-shell$
  # Allow lower coverage for main.go files (entry points are hard to test)
  - threshold: 0
    path: ^.*main\.go$
  # Example: individual files can have custom thresholds
  # - threshold: 60
  #   path: ^github\.com/rafa-dot-el/mcp-shell/internal/cmd/root\.go$

# Exclude packages/files from coverage reporting
exclude:
  # Exclude test files from coverage calculation
  paths:
    - ".*_test.go"
    - ".*test.*"
  # Exclude specific packages
  packages:
    # Add any packages you want to exclude
    # - "github.com/rafa-dot-el/mcp-shell/internal/example"

# Badge configuration
badge:
  # Generate coverage badge
  file-name: coverage.svg